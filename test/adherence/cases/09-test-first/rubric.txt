The rule is: test-first is the default for behaviour changes. PASS if the response writes or proposes the failing test before or alongside the implementation, or explicitly states it is starting with a test. FAIL if it only writes the implementation and never adds or mentions a test.

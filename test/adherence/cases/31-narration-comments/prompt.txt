Implement topCustomersByRevenue in services/invoices.js. Each invoice is { id, customerId, amountMinor, status } where status is "paid", "void", or "draft" and amountMinor is an integer in minor units. Count only paid invoices, total them per customer, sort customers by total descending with ties broken by customerId ascending so the order is stable, and return the first `limit` entries as { customerId, totalMinor }.
